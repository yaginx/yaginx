import React, { useEffect, useRef } from "react";
interface Props {
    value: string | undefined;
    onChange: (value: any) => void;
}

export const TinymceEditor: React.FC<Props> = (props) => {
    return (
        <>

        </>
    )
};
